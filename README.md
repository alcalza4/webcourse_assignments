# Website
The website was deployed to my own server, so you won't be able to see it, but all the backend pushes the files using the static dist.

--watch doesn't work properly, so I am using nodemon instaed...